{
  "name": "lzycrazy-brand",
  "version": "1.0.0",
  "scripts": {
    "admin": "npm run dev --prefix admin",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run frontend\" \"npm run admin\" \"npm run backend\""
  },
  "dependencies": {
    "concurrently": "^9.2.0"
  }
}
